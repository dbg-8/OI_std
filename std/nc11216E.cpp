#include <stdio.h>

int n;

int main () {
	scanf("%d", &n);
	int ret = 0, ans = 0;
	for (int i = 1; i <= n; i++) {
		int x;
		scanf("%d", &x);
		int ret = 0;
		for (int i = 1; i <= )
	}
	printf("%d\n", ans);
	return 0;
}
