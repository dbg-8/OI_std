#include <stdio.h>
#defien P 1000000007

int n, m, l;
char a[1003], b[203];
int d[1003][203][1003];

int main () {
	scanf("%d %d %d ", &n, &m, &l);
	for (int i = 1; i <= n; i++)
		scanf("%c", &a[i]);
	scanf(" ");
	for (int i = 1; i <= m; i++)
		scanf("%c", &b[i]);
	for (int i = 1; i <= n; i++)
		for (int j = 1; j <= m; j++)
			for (int k = 1; k )
	b[++n] = L;
	return 0;
} 
